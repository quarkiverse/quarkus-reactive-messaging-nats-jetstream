# Root logger
logger.level=INFO
logger.handlers=CONSOLE

# Register custom loggers (REQUIRED by JBoss LogManager)
loggers=io.quarkiverse.reactive.messaging.nats.jetstream,org.testcontainers

# Configure logger levels
logger.io.quarkiverse.reactive.messaging.nats.jetstream.level=INFO
logger.org.testcontainers.level=INFO

# Console Handler
handler.CONSOLE=org.jboss.logmanager.handlers.ConsoleHandler
handler.CONSOLE.level=INFO
handler.CONSOLE.formatter=PATTERN
handler.CONSOLE.properties=autoFlush,target
handler.CONSOLE.autoFlush=true
handler.CONSOLE.target=SYSTEM_OUT

# Formatter
formatter.PATTERN=org.jboss.logmanager.formatters.PatternFormatter
formatter.PATTERN.properties=pattern
formatter.PATTERN.pattern=%d{HH:mm:ss,SSS} %-5p [%c{1.}] (%t) %s%e%n